/**
 * Custom Platform Upload Adapter (future)
 *
 * Implements the uploadAuction() interface for the custom auction platform.
 * Write this when the custom platform API is available.
 * Nothing else in this repo changes when swapping adapters.
 */

/** @param {Object} auctionJson @param {string} folderPath @param {Object} config @param {Function} onProgress */
export async function uploadAuction(auctionJson, folderPath, config, onProgress) {
  // TODO: implement custom platform upload
  throw new Error('Custom platform adapter not yet implemented');
}
